---
title: PropertyValue
---

<!-- Edited by human: false -->
<!-- Examples count: 2 -->
<!-- Source examples: 0 -->
<!-- Playground examples: 0 -->
<!-- External examples: 2 -->
<!-- Import examples: 2 -->

## Styling Guidelines
* All snippets use Tailwind CSS.
* No additional Contember theme classes are required.

## Core Concepts
* PropertyValue displays a single value inside a PropertyList.
* You combine PropertyValue with PropertyRow or PropertyItem for aligned layouts.
* Children define the rendered content; no extra props are needed.

## Quick Start
```tsx
import { PropertyList, PropertyRow, PropertyLabel, PropertyValue } from '~/lib/ui/property-list'

export const EmailPreview = () => (
	<PropertyList>
		<PropertyRow>
			<PropertyLabel className="font-semibold">Subject</PropertyLabel>
			<PropertyValue className="font-normal">Quarterly Report</PropertyValue>
		</PropertyRow>
	</PropertyList>
)
```

## Sub-Components / Building Blocks
### PropertyList
* Wraps a group of property rows or items.
* Provides vertical spacing.

### PropertyRow
* Aligns a label and value horizontally.

### PropertyLabel
* Renders the left-hand label.
* Usually bold or semibold.

### PropertyItem
* Shortcut that combines label and children in one tag.

## Examples
```tsx
import {
	PropertyList,
	PropertyItem,
	PropertyRow,
	PropertyLabel,
	PropertyValue,
} from '~/lib/ui/property-list'

export const EmailDetail = () => (
	<PropertyList>
		<PropertyItem label="Recipients">john@example.com, jane@example.com</PropertyItem>

		<PropertyItem label="Subject">New Features</PropertyItem>

		<PropertyRow>
			<PropertyLabel>Content</PropertyLabel>
			<PropertyValue className="font-normal">
				<p className="whitespace-pre-line">
					Hi team,

					Check out the latest improvements…
				</p>
			</PropertyValue>
		</PropertyRow>

		<PropertyItem label="Sent At">2023-04-05 14:32</PropertyItem>
	</PropertyList>
)
```
Above you render an email record with labels and values neatly aligned.

## Best Practices & Gotchas
* Keep labels short; wrap long text inside PropertyValue, not PropertyLabel.
* Add `className="font-normal"` to PropertyValue to reset inherited bold styles.
* Group related rows in one PropertyList for consistent spacing.

## Further Reading
* PropertyItem
* PropertyLabel
* PropertyList
* PropertyRow
* Contember Field components for data binding.

<!-- End of Generated Documentation -->
