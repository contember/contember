---
title: TableRowElementRenderer
---

<!-- Edited by human: false -->
<!-- Examples count: 0 -->
<!-- Source examples: 0 -->
<!-- Playground examples: 0 -->
<!-- External examples: 0 -->
<!-- Import examples: 0 -->

# TableRowElementRenderer

## Overview
TableRowElementRenderer renders a single table row (`<tr>`) inside the Contember Slate editor.  
You pass it a `TableRowElement` object and it outputs the correct DOM structure for the editor.

## Styling Guidelines
* All examples use Tailwind CSS utility classes.  
* No Contember theme classes are required.

## Core Concepts
* Slate represents table rows with `TableRowElement` objects.
* TableRowElementRenderer is a low-level renderer—provide the element, receive a row.
* You typically use it inside a custom table renderer or editor plugin.

## Quick Start
```tsx
import { TableRowElementRenderer } from './elements/TableRowElementRenderer'

/* “element” comes from your Slate table logic */
const MyTableRow = () => (
  <div className="prose">
    <TableRowElementRenderer element={element} />
  </div>
)
```

## Detailed Guide
_None_

## Props Reference
| Name    | Type | Required | Default | Description |
|---------|------|----------|---------|-------------|
| element | TableRowElement | Yes | — | Slate representation of the row to render |

### Inherited Props
_None_

## Sub-Components / Building Blocks
_None_

## Examples
_None_

## Best Practices & Gotchas
* Always pass a valid `TableRowElement`; other objects cause render errors.
* Wrap the renderer in a styled container instead of passing style props—TableRowElementRenderer does not forward `className`.
* Memoize the component when rendering many rows to avoid unnecessary re-renders.

## Further Reading
* Contember Slate Table guide  
* `TableCellElementRenderer` for individual cells  
* `TableElementRenderer` for complete tables

<!-- End of Generated Documentation -->