---
title: UploadedAudioView
---

<!-- Edited by human: false -->
<!-- Examples count: 8 -->
<!-- Source examples: 0 -->
<!-- Playground examples: 0 -->
<!-- External examples: 8 -->
<!-- Import examples: 5 -->

# UploadedAudioView

## Overview
UploadedAudioView renders a single audio file stored in Contember.  
You can preview the track, display metadata, and plug in custom edit or destroy actions.

## Styling Guidelines
* All examples use Tailwind CSS.  
* The component adds no classes. Wrap it with your own Tailwind containers or Contember theme utilities as needed.

## Core Concepts
* Audio metadata lives in Contember entity fields.
* Map entity fields to props like `urlField="url"`.
* Use `actions`, `edit`, or `DestroyAction` to extend the default UI.
* Toggle the built-in destroy button with `noDestroy`.

## Quick Start
```tsx
import { UploadedAudioView } from '@contember/react-components'

export const SongPreview = () => (
  <div className="p-4 bg-white rounded shadow">
    <UploadedAudioView urlField="url" />
  </div>
)
```

## Detailed Guide
_None_

## Props Reference
| Name | Type | Required | Default | Description |
|------|------|----------|---------|-------------|
| urlField | string &#124; SugarFieldConfig | Yes | — | Entity field with the public URL of the audio file. |
| fileNameField | string &#124; SugarFieldConfig | No | — | Entity field holding the original file name. |
| lastModifiedField | string &#124; SugarFieldConfig | No | — | Entity field with the last modified timestamp. |
| fileSizeField | string &#124; SugarFieldConfig | No | — | Entity field containing file size (bytes). |
| fileTypeField | string &#124; SugarFieldConfig | No | — | Entity field with MIME type. |
| durationField | string &#124; SugarFieldConfig | No | — | Entity field with audio length in seconds. |
| baseField | string &#124; SugarRelationConfig | No | — | Optional relation path to the base file entity. |
| actions | React.ReactNode | No | — | Extra buttons or links shown next to default actions. |
| edit | React.ReactNode | No | — | Custom edit UI rendered in place of the component when triggered. |
| noDestroy | boolean | No | false | Prevents rendering the default destroy button. |
| DestroyAction | React.ComponentType&lt;{ children: React.ReactElement }&gt; | No | — | Custom component wrapping the destroy button. |

## Sub-Components / Building Blocks
_None_

## Examples

### 1. Custom Actions
Add a download button while keeping the default destroy action.

```tsx
<UploadedAudioView
  urlField="url"
  actions={
    <a
      href={record.url}
      download
      className="text-blue-600 hover:underline"
    >
      Download
    </a>
  }
/>
```

The link appears next to the built-in controls. Tailwind styles keep visual consistency.

### 2. Replace Destroy Action
Wrap the destroy trigger with your own component.

```tsx
const DangerButton = ({ children }: { children: React.ReactElement }) => (
  <span className="text-red-600 hover:text-red-800">{children}</span>
)

<UploadedAudioView
  urlField="url"
  DestroyAction={DangerButton}
/>
```

You changed only the visuals; Contember still handles deletion logic.

### 3. Repeater With Remove Trigger (Real-World)
```tsx
<FileRepeaterFieldInner
  {...props}
  fileType={createAudioFileType(props)}
>
  {props.children ?? (
    <UploadedAudioView
      {...props}
      DestroyAction={RepeaterRemoveItemTrigger}
    />
  )}
</FileRepeaterFieldInner>
```

Files live inside a repeater.  
`RepeaterRemoveItemTrigger` integrates with Contember form state, so removing an item updates both UI and data.

### 4. Single Upload Field With Disconnect Trigger (Real-World)
```tsx
<UploadFieldInner
  {...props}
  fileType={createAudioFileType(props)}
>
  {props.children ?? (
    <UploadedAudioView
      {...props}
      DestroyAction={DisconnectEntityTrigger}
    />
  )}
</UploadFieldInner>
```

Use this layout for one-off uploads.  
`DisconnectEntityTrigger` keeps the record but unlinks the audio file.

### 5. Repeater Item Wrapped in UploaderItemUI (Real-World)
```tsx
<UploadFieldInner {...props} fileType={createAudioFileType(props)}>
  <UploaderItemUI>
    {props.children ?? (
      <UploadedAudioView
        {...props}
        DestroyAction={DisconnectEntityTrigger}
      />
    )}
  </UploaderItemUI>
</UploadFieldInner>
```

`UploaderItemUI` supplies drag handles and consistent padding.  
The view stays unchanged; the wrapper handles layout concerns.

### 6. Inline Repeater Variation (Real-World)
```tsx
<>
  <FileRepeaterFieldInner {...props} fileType={createAudioFileType(props)}>
    {props.children ?? (
      <UploadedAudioView
        {...props}
        DestroyAction={RepeaterRemoveItemTrigger}
      />
    )}
  </FileRepeaterFieldInner>
</>
```

This minimal variation shows how teams embed the component directly in fragments for terser JSX.

## Best Practices & Gotchas
* Always pass `urlField`; the component throws if missing.  
* Wrap the player in a fixed-width container to prevent stretching.  
* Use `noDestroy` when the user lacks delete permission.  
* Keep custom actions small; they share horizontal space.  
* In repeaters, prefer a `DestroyAction` wired to form helpers (see real-world examples).

## Further Reading
* Contember File Upload pipeline  
* `<UploadField>` – component used to upload audio files  
* Contember access control  

<!-- End of Generated Documentation -->