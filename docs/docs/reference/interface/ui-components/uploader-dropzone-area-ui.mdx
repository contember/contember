---
title: UploaderDropzoneAreaUI
---

<!-- Edited by human: false -->
<!-- Examples count: 2 -->
<!-- Source examples: 0 -->
<!-- Playground examples: 2 -->
<!-- External examples: 0 -->
<!-- Import examples: 1 -->

## Overview
UploaderDropzoneAreaUI renders the visible “drop files here” zone inside a Contember uploader.  
It only controls layout and styling; upload logic lives in surrounding uploader components.

## Styling Guidelines
* All examples use Tailwind CSS.  
* The component adds no Contember-specific theme classes—style it freely with Tailwind utilities.

## Core Concepts
* The component is **presentational**—place your own icons, text, or buttons inside.  
* It forwards click and drop events to the surrounding uploader context.  
* Combine it with `UploaderDropzoneWrapperUI` and `UploaderInactiveDropzoneUI` for full UX.

## Quick Start
```tsx
import { UploaderDropzoneAreaUI } from '~/lib/upload'
import { UploadIcon } from '@radix-ui/react-icons'

export default function ImageUploaderPlaceholder() {
	return (
		<UploaderDropzoneAreaUI className="flex flex-col items-center gap-2 p-6 border-2 border-dashed rounded-lg">
			<UploadIcon className="w-12 h-12 text-gray-400" />
			<span className="text-sm font-semibold">Drop images here</span>
			<span className="text-xs text-gray-500">or click to browse</span>
		</UploaderDropzoneAreaUI>
	)
}
```

## Detailed Guide
_None_

## Props Reference
_None_

## Sub-Components / Building Blocks
_None_

## Examples

### Playground – Single Image Field
```tsx
<Binding>
	<Slots.Title>
		<Title icon={<UploadIcon />}>Image upload</Title>
	</Slots.Title>

	<Slots.Actions>
		<PersistButton />
	</Slots.Actions>

	<EntitySubTree entity="UploadRoot(unique = unique)" setOnCreate="(unique = unique)">
		<ImageField
			baseField="image"
			{...imageFields}
			label="Image file"
			description="Some description of the image file."
			dropzonePlaceholder={(
				<UploaderDropzoneAreaUI className="w-60">
					<UploadIcon className="w-12 h-12 text-gray-400" />
					<div className="font-semibold text-sm">{dict.uploader.dropFiles}</div>
					<div className="text-xs">{dict.uploader.or}</div>
					<div className="flex gap-2 items-center text-xs">
						<Button size="sm" variant="outline">{dict.uploader.browseFiles}</Button>
						<div onClick={e => e.stopPropagation()}>
							<SelectImage />
						</div>
					</div>
				</UploaderDropzoneAreaUI>
			)}
		/>
	</EntitySubTree>
</Binding>
```
The placeholder appears only when no image is selected.  
Nested inputs like `SelectImage` stay interactive because their click events are stopped.

### Playground – Image Repeater Field
```tsx
<Binding>
	<Slots.Title>
		<Title icon={<UploadIcon />}>Image repeater upload</Title>
	</Slots.Title>

	<Slots.Actions>
		<PersistButton />
	</Slots.Actions>

	<EntitySubTree entity="UploadRoot(unique = unique)" setOnCreate="(unique = unique)">
		<ImageRepeaterField
			field="imageList.items"
			baseField="image"
			sortableBy="order"
			{...imageFields}
			label="Image file"
			description="Some description of the image file."
			dropzonePlaceholder={(
				<UploaderDropzoneAreaUI className="w-60">
					<UploadIcon className="w-12 h-12 text-gray-400" />
					<div className="font-semibold text-sm">{dict.uploader.dropFiles}</div>
					<div className="text-xs">{dict.uploader.or}</div>
					<div className="flex gap-2 items-center text-xs">
						<Button size="sm" variant="outline">{dict.uploader.browseFiles}</Button>
						<div onClick={e => e.stopPropagation()}>
							<SelectImageRepeater />
						</div>
					</div>
				</UploaderDropzoneAreaUI>
			)}
		/>
	</EntitySubTree>
</Binding>
```
Each repeater item uses its own dropzone, letting users add many images quickly.  
Sorting and other list controls remain functional.

### Production – Conditional Inactive State
```tsx
{isUploading ? (
	<UploaderInactiveDropzoneUI />
) : (
	<UploaderDropzoneArea>
		<UploaderDropzoneAreaUI>
			<UploadIcon className="w-12 h-12 text-gray-400" />
			{/* … */}
		</UploaderDropzoneAreaUI>
	</UploaderDropzoneArea>
)}
```
Swap the active area for `UploaderInactiveDropzoneUI` during file uploads to prevent additional drops.

## Best Practices & Gotchas
* Always render at least one child so the dropzone has a visible height.  
* Stop event propagation on nested file inputs to keep them clickable.  
* Disable the zone while uploads are in progress to avoid duplicate files.  
* Wrap the component in `UploaderDropzoneWrapperUI` for keyboard focus outlines.

## Further Reading
* `UploaderDropzoneWrapperUI` – outer container with focus ring.  
* `UploaderInactiveDropzoneUI` – read-only overlay during uploads.  
* Contember Uploader guide in the official docs.

<!-- End of Generated Documentation -->