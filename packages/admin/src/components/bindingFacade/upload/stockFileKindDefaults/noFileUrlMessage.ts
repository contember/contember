export const noFileUrlMessage =
	`Trying to render a persisted file but its URL has not been configured.	This may have happened because the file url ` +
	`field has not been configured or has been configured for a different file kind. Alternatively, it could also ` +
	`happen through use of a custom set of file data populators but without a corresponding renderFile callback.`
