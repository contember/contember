{
  "scripts-template": {
    "contember": "contember",
    "start": "contember migrations:execute . --yes && vite admin --port ${VITE_PORT:-4000} --host 0.0.0.0",
    "build": "tsc && vite build admin"
  },
  "devDependencies": {
    "@contember/cli": "{version}",
    "@contember/schema": "{version}",
    "@contember/schema-definition": "{version}",
    "@contember/admin": "^1.0.0-alpha.6",
    "@types/react": "^17",
    "react": "^17",
    "react-dom": "^17",
    "typescript": "^4.5",
    "vite": "^2.7"
  }
}
