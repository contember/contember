import { GenericPage, TitleBar } from '@contember/admin'
import * as React from 'react'

export const DashboardPage = (
	<GenericPage pageName="dashboard">
		<TitleBar>Sandbox admin</TitleBar>
	</GenericPage>
)
